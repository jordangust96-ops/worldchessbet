{
  "name": "EmailLog",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "ID of the user this email concerns"
    },
    "recipient_email": {
      "type": "string",
      "description": "Address the email was sent (or attempted) to"
    },
    "email_type": {
      "type": "string",
      "enum": [
        "welcome",
        "founding_player",
        "deposit_available",
        "withdrawal_requested",
        "match_accepted"
      ],
      "description": "Which automated email this log entry is for. New values are added here as more automated emails are built."
    },
    "subject": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "success",
        "failed",
        "skipped"
      ]
    },
    "error_message": {
      "type": "string",
      "description": "Error details if delivery failed, for administrative review"
    },
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional description"
    },
    "match_id": {
      "type": "string",
      "description": "Match associated with this transactional notification."
    },
    "reason": {
      "type": "string",
      "description": "Machine-readable send, skip, or failure reason. Success means provider acceptance, not inbox delivery."
    }
  },
  "required": [
    "recipient_email",
    "email_type",
    "status"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": false,
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}
