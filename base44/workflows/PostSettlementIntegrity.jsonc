{
  "name": "Post-Settlement Integrity",
  "description": "After completion, runs lightweight integrity checks. Money games receive Stockfish screening; free games request ratings immediately and skip Stockfish.",
  "trigger": {
    "config": {
      "trigger_type": "entity",
      "entity_name": "Match",
      "events": [
        "update"
      ]
    },
    "condition": "${ .trigger.data.status == \"completed\" and .trigger.old_data.status != \"completed\" }"
  },
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "post_settlement_integrity",
      "version": "1.0",
      "namespace": "base44"
    },
    "do": [
      {
        "run_post_settlement_jobs": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "runPostSettlementJobs",
            "args": {
              "matchId": "${ .trigger.entity_id }",
              "gameId": "${ .trigger.data.game_id }"
            }
          },
          "then": "end",
          "x-base44": {
            "title": "Run Post-Settlement Jobs",
            "description": "Dispatch post-game checks and free-game ratings outside the completion response"
          }
        }
      }
    ]
  }
}